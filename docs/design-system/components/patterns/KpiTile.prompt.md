Metric tile — uppercase label, 30px tabular value, optional hint. The KPI card from the daily briefing strip.

```jsx
<KpiTile label="Oportunidades 3★" value={7} hint="alta confiança" tone="forest" />
```

Tone colors the value: `ink` (default), `forest` (positive), `amber` (attention), `danger`.
