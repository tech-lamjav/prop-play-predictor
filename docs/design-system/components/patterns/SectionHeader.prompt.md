List-section header — uppercase eyebrow, 18px title (with optional muted count), and a right-aligned action link.

```jsx
<SectionHeader eyebrow="Jogos de hoje" title="4 partidas" actionLabel="Ver todos" actionHref="/home-games" />
```
