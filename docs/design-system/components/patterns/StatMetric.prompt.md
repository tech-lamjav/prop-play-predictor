Hero metric cell — tiny label, 22px tabular value, sub caption. Row several inside a bordered strip (the "Média / Projeção / Linha / Vantagem" block). Set `onDark` on the forest hero.

```jsx
<StatMetric label="Projeção" value="28.4" sub="com filtro" highlight />
<StatMetric label="Vantagem" value="+15%" sub="sobre linha" accent />
```
