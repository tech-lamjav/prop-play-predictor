Small status pill or tag. Forest chip by default; semantic status tones (success / warning / danger / info) and neutral/outline. Use `square` for filter tags.

```jsx
<Badge tone="forest">3★</Badge>
<Badge tone="warning" square>OUT</Badge>
<Badge tone="success">+15% edge</Badge>
```
