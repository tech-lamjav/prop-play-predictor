Text or search input — white surface, hairline border, forest focus ring. Pass `leadingIcon` for the search-field pattern used across the app.

```jsx
<Input placeholder="Buscar jogador..." leadingIcon={<Search size={16} />} />
```
